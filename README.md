# cert-str-x509-details
Take a simple PEM string (unformatted) and get the x509 details from it

Usage:
```
CERT='<cert_string_here>'
docker run ghcr.io/lordtatty/cert-str-x509-details:main $CERT
```
